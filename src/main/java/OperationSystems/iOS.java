package OperationSystems;

import java.time.LocalDate;

public class iOS extends OS{
    public iOS(String name, String startUpTime, String bios, LocalDate manufactureDate, String version) {
        super(name, startUpTime, bios, manufactureDate, version);
    }
}
